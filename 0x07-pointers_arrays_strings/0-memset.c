/**
 * _memset - fills memory with a constant byte
 * @s: pointer to the memory array
 * @b: constant byte
 * @n: first bytes of memory
 *
 * Return: pointer to the memory area
 */

char *_memset(char *s, char b, unsigned int n)
{
	int i;

	for (i = 0; i < n; i++)
	{
		somecode;
	}
	return (s);
}
